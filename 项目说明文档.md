# 企业级技能增长管理系统 (SkillTree Management System)

## 1. 项目概述
本系统是一套专为企业设计的数字化技能培养与成长追踪平台。它通过将复杂的技能体系转化为直观的可视化“技能树”，实现了从技能建模、个性化学习路径规划、到全员技能达成率监控的闭环管理。

系统解决了企业人才培养中“技能标准不统一”、“学习进度不透明”、“任务分发不精准”的核心痛点，为管理层提供了科学的数字化决策支持。

## 2. 技术栈与架构设计
*   **开发语言**：Python 3.10+ / JavaScript (ES6+) / HTML5 / CSS3
*   **架构设计**：
    *   **后端**：基于 Flask 框架的 RESTful API 架构，支持轻量级高并发请求。
    *   **数据库**：SQLite 3 处理持久化存储，结合 SQLAlchemy ORM 确保数据访问的安全性与一致性。
    - **前端表现层**：Vanilla JS 原生渲染，无第三方框架依赖，确保毫秒级响应速度；全案采用 Slate 低饱和度设计规范。

## 3. 功能点深度细化说明

### 3.1 技能树可视化建模 (Modeling Engine)
*   **画布级导图编辑**：
    - 支持节点自由扩展（正向/反向布局）、拖拽式结构调整、智能对齐。
    - **样式持久化技术**：在调整树形结构或移动节点顺序后，系统自动保留用户定义的颜色、字体及层级属性，防止数据丢失。
*   **原子级节点属性定义**：
    - **元数据绑定**：每个节点可绑定详细的技能描述、双学习链接（如支持内网文档链接与外部教学视频）、技能点权重。
    - **层级自动标识**：系统自动计算节点深度（Level 1-5），辅助建立初级到高级的晋升阶梯。

### 3.2 精准任务生命周期管理 (Task Engine)
*   **多维任务分配逻辑**：
    - **层级路径追溯**：解决多层级下“重名节点”的定位问题。分配时系统会自动显示 `父节点 > 子节点` 的全路径，确保分配不偏航。
    - **状态实时感知**：分配列表会实时标记该用户已有的任务状态（如：`已分配` 标红），防止重复下达任务。
*   **任务分类与周期**：
    - **季度/年度拆分**：支持将技能点划分为“本季核心目标”与“长期成长计划”，在用户看板中实现分区显示，提升学习优先级意识。
*   **管理撤回闭环**：
    - **任务一键回滚**：管理员或组长可随时在后台撤回不合理的任务分配，用户前端看板同步刷新，实现敏捷管理。

### 3.3 交互式管理大盘 (BI Dashboard)
*   **全景视图统计**：
    - 实时统计全员人数、树总量、系统内激活总频次，通过数据波动反映企业学习热度。
*   **下钻式数据透视 (Data Drill-Down)**：
    - **模块概览**：按 MOS、TC、EES 等独立模块切分进度。点击进度条可直接钻取到该模块下的成员列表及其各自进度。
    - **小组对比**：A/B/C/D 各小组横向 PK，激活率、平均进度一目了然，辅助组长发现组内弱项。
*   **学习行为热力分析**：
    - 记录并展示全站“最受关注节点”和“最高频激活节点”，指导企业知识库的内容优化方向。

### 3.4 员工端学习看板 (Learner Interface)
*   **双布局模式**：支持“树形全景”观察全局和“垂直清单”专注具体工作任务，适配不同的阅读习惯。
*   **进度实时反馈**：用户点击“申请激活”后，任务状态从“任务中”流转至“已达成”，进度条自动平滑推进。

### 3.5 集团化用户与权限中心 (User & ACL)
*   **多角色权限矩阵**：
    - **管理员 (Admin)**：全局配置、技能建模、全员分配、数据底层操作。
    - **组长 (Leader)**：管理指定小组（A/B/C/D）的进度查阅权、任务分配/撤回。
    - **普通用户 (User)**：查看个人分配任务、申请技能点激活。
*   **增强型列表管理**：支持基于“用户名”的实时搜索、基于“组别”及“职权（角色）”的多维过滤，在处理百人以上量级时依然高效。

## 4. 后续扩展蓝图 (Roadmap)
*   **附件直传系统**：支持将学习成果证明直接上传至技能节点。
*   **自动化通知通知**：任务超时或达成时，通过钉钉/企业微信 API 进行即时推送。
*   **人才画像分析**：基于各维度的技能点达成情况，自动生成员工个人能力雷达图。

---
*文档更新日期：2024-05-03 汇报专用版*
