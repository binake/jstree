# 前台节点信息显示方案

## 方案概述
为前台页面设计节点 description（技能说明）和 link（相关链接）的显示方案，提供多种交互方式，提升用户体验。

## 方案一：综合交互方案（推荐）

### 1. 节点图标提示
- **位置**：节点右上角显示小图标
- **图标规则**：
  - 如果有 description：显示 📝 图标
  - 如果有 link：显示 🔗 图标
  - 如果两者都有：显示 📝🔗 两个图标
- **样式**：半透明背景，鼠标悬停时高亮

### 2. 鼠标悬停 Tooltip（快速预览）
- **触发**：鼠标悬停在节点上
- **显示内容**：
  - 节点名称
  - 状态和消耗
  - Description（如果有，最多显示3行，超出显示"..."）
  - Link（如果有，显示为可点击链接）
- **样式**：深色半透明背景，金色边框，圆角卡片
- **位置**：跟随鼠标，自动调整位置避免超出屏幕

### 3. 点击节点详细信息面板（侧边栏）
- **触发**：点击节点
- **显示位置**：右侧滑出面板（宽度 350px）
- **显示内容**：
  - 节点名称（大标题）
  - 状态徽章（锁定/已解锁/已激活）
  - 消耗技能点
  - Description（完整内容，支持多行）
  - Link（大按钮样式，新窗口打开）
  - 关闭按钮
- **动画**：从右侧滑入，带阴影效果
- **响应式**：移动端全屏显示

### 4. 节点信息卡片（可选）
- **触发**：双击节点
- **显示位置**：节点旁边弹出卡片
- **显示内容**：同侧边栏，但以卡片形式显示
- **样式**：半透明背景，金色边框，带阴影

## 方案二：简洁方案

### 1. 仅 Tooltip
- 鼠标悬停显示完整信息
- 包含 description 和 link
- 简单直接，不占用额外空间

### 2. 仅侧边栏
- 点击节点显示详细信息
- 不显示图标提示
- 界面更简洁

## 方案三：图标 + Tooltip 组合

### 1. 节点图标
- 节点上显示信息图标（📝 或 🔗）
- 图标可点击

### 2. Tooltip
- 鼠标悬停图标显示详细信息
- 包含完整 description 和 link

## 推荐实现：方案一（综合交互方案）

### 优势：
1. **多层次信息展示**：图标提示 → 悬停预览 → 点击详情
2. **用户体验好**：不同场景使用不同交互方式
3. **信息完整**：既支持快速预览，也支持详细查看
4. **视觉清晰**：图标提示让用户知道哪些节点有额外信息

### 实现细节：

#### 1. 节点图标样式
```css
.node-info-icon {
    position: absolute;
    top: -8px;
    right: -8px;
    width: 20px;
    height: 20px;
    background: rgba(255, 215, 0, 0.8);
    border-radius: 50%;
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 12px;
    cursor: pointer;
    z-index: 10;
    transition: all 0.3s;
}

.node-info-icon:hover {
    transform: scale(1.2);
    background: rgba(255, 215, 0, 1);
    box-shadow: 0 0 10px rgba(255, 215, 0, 0.8);
}
```

#### 2. Tooltip 样式
```css
.node-tooltip {
    max-width: 300px;
    line-height: 1.6;
}

.node-tooltip .description {
    max-height: 60px;
    overflow: hidden;
    text-overflow: ellipsis;
    display: -webkit-box;
    -webkit-line-clamp: 3;
    -webkit-box-orient: vertical;
}
```

#### 3. 侧边栏样式
```css
.node-detail-panel {
    position: fixed;
    right: -400px;
    top: 0;
    width: 350px;
    height: 100vh;
    background: rgba(26, 26, 46, 0.95);
    backdrop-filter: blur(10px);
    border-left: 3px solid #FFD700;
    padding: 20px;
    overflow-y: auto;
    transition: right 0.3s ease;
    z-index: 1000;
    box-shadow: -5px 0 20px rgba(0,0,0,0.5);
}

.node-detail-panel.show {
    right: 0;
}
```

#### 4. 数据结构
```javascript
// 节点数据示例
{
    id: "node_123",
    topic: "技能名称",
    data: {
        description: "这是技能的详细说明...",
        link: "https://example.com/tutorial",
        cost: 5,
        status: "activated"
    }
}
```

## 交互流程

1. **用户浏览技能树**
   - 看到有 📝 或 🔗 图标的节点，知道有额外信息

2. **快速预览**
   - 鼠标悬停节点 → 显示 Tooltip（简要信息）

3. **查看详情**
   - 点击节点 → 右侧滑出详细信息面板
   - 可以查看完整 description
   - 可以点击链接跳转

4. **关闭面板**
   - 点击关闭按钮或点击面板外部区域

## 移动端适配

- **Tooltip**：改为底部弹出卡片
- **侧边栏**：改为全屏弹窗
- **图标**：适当放大，便于点击

## 技术实现要点

1. **事件处理**：
   - 监听节点点击事件
   - 监听鼠标悬停事件
   - 处理面板关闭事件

2. **数据获取**：
   - 从节点 data 中读取 description 和 link
   - 检查数据是否存在

3. **样式应用**：
   - 动态添加图标到节点
   - 动态显示/隐藏 Tooltip
   - 控制侧边栏显示/隐藏

4. **性能优化**：
   - Tooltip 延迟显示（300ms）
   - 使用 CSS 动画而非 JS 动画
   - 避免频繁 DOM 操作

## 后续扩展

1. **搜索功能**：在侧边栏添加搜索框，搜索 description 内容
2. **收藏功能**：允许用户收藏常用链接
3. **分享功能**：分享节点信息
4. **历史记录**：记录用户查看过的节点

